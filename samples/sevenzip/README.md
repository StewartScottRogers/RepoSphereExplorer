# readings.7z

An archive holding nested directories and four files, written by py7zr.

7-Zip packs several files into one *solid* block, so the compressed size
of any single entry is not a thing that exists — only the block's is. The
pane says which entries share a block, because extracting one of them
means decompressing all of them.
