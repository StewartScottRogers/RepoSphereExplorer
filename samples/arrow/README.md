# readings.arrow

The same nine hundred readings in the Arrow file format, written as three
record batches.

The `station` column is dictionary encoded — four distinct values across
nine hundred rows — and the schema carries custom metadata. Both are
things the pane should say, because neither is visible from the row
values alone.
