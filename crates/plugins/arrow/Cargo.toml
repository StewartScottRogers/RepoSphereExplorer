[package]
name = "plugin-arrow"
description = "Apache Arrow file type plugin: core and presentation halves."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
plugin-api = { path = "../../plugin-api" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
arrow = { version = "59", default-features = false, features = ["ipc", "prettyprint"] }

[lints]
workspace = true
