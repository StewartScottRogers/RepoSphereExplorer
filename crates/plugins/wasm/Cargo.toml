[package]
name = "plugin-wasm"
description = "WebAssembly module file type plugin: core and presentation halves."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
plugin-api = { path = "../../plugin-api" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
wasmparser = { version = "0.225", default-features = false, features = ["std"] }

[lints]
workspace = true
