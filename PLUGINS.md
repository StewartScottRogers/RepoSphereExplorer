# Plugin registry

Tracks every file-type plugin this project has built or rejected, per
[GUIDANCE.md §3.1](GUIDANCE.md#31-plugin-architecture) and
[§7 D5](GUIDANCE.md#7-open-decisions). Consulted before proposing or
building any plugin work order, so the factory never duplicates a plugin
that already exists or retries a format already ruled out.

## Built

| Format | Crate | Issue | Notes |
| --- | --- | --- | --- |
| Plain text | `crates/plugins/text` | — | First plugin, built with the architecture itself |
| Image (raster codecs) | `crates/plugins/image` | — | |
| Archive (zip) | `crates/plugins/archive` | — | Also provides `extract` for the D4 operations |
| PDF | `crates/plugins/pdf` | — | |
| Directory-as-file | `crates/plugins/directory` | — | |
| Python source | `crates/plugins/python` | #7 | Sniffs by shebang or top-level `def`/`class`, ahead of `text` in `CORE_PLUGINS` |
| JavaScript source | `crates/plugins/javascript` | #8 | Sniffs by `node` shebang, top-level `function`/`class`, or CommonJS/ES-module/arrow-function markers, ahead of `text` in `CORE_PLUGINS` |
| TypeScript source | `crates/plugins/typescript` | #9 | Sniffs by TypeScript-only markers (`interface`/`enum`/type-alias declarations, type annotations, visibility modifiers, `implements`, `import type`), ahead of `javascript` in `CORE_PLUGINS` so it claims TS-specific syntax first |
| Rust source | `crates/plugins/rust` | #10 | Sniffs by `fn`/`struct`/`enum`/`trait`/`impl` declarations and markers (`let mut`, `println!(`, `#[derive(`, `use std::`) not used by this project's other source-language plugins; placed just ahead of `text` in `CORE_PLUGINS` |
| Go source | `crates/plugins/go` | #11 | Sniffs by `package`/`func`/`import (` declarations and markers (`:=`, `fmt.Println(`, `fmt.Printf(`) not used by this project's other source-language plugins; placed just ahead of `text` in `CORE_PLUGINS` |
| C source | `crates/plugins/c` | #12 | Sniffs by `#include <...>`/`#include "..."` directives and markers (`int main(`, `void main(`, `printf(`, `malloc(`, `NULL`) not used by this project's other source-language plugins; placed just ahead of `text` in `CORE_PLUGINS`. No path/extension-based dispatch exists in this architecture (sniffing is content-only), so disambiguating `.c`/`.h` from a future C++ plugin will need that plugin's sniff to avoid these same markers, or to be ordered after `c` |
| C++ source | `crates/plugins/cpp` | #13 | Sniffs by C++-only markers (`#include <iostream>`/`<vector>`/`<string>`, `class `, `namespace `, `std::`, `cout <<`, `cin >>`, `nullptr`, `public:`/`private:`/`protected:`, `template<`) that avoid the C plugin's markers per its note, so a C++ file that also contains C-style constructs (`int main(`, `printf(`) is still claimed correctly; placed just ahead of `c` in `CORE_PLUGINS` |
| C# source | `crates/plugins/csharp` | #14 | Sniffs by C#-only markers (`using System`, `Console.WriteLine(`/`Console.Write(`, `public class `/`internal class `, `public static void Main(`/`static void Main(`, `{ get; set; }`) that avoid the C++ plugin's overlapping `class `/`namespace ` markers, so a C# file whose `namespace` block also matches C++'s bare check is still claimed correctly; placed just ahead of `cpp` in `CORE_PLUGINS` |
| Java source | `crates/plugins/java` | #15 | Sniffs by Java-only markers (`import java.`, `System.out.println(`/`System.out.print(`/`System.err.println(`, `public static void main(String`, `@Override`) that avoid the C#'s bare `public class ` marker and the Go plugin's bare `package ` marker, so a Java file is still claimed correctly rather than by either; placed just ahead of `csharp` in `CORE_PLUGINS` |
| Kotlin source | `crates/plugins/kotlin` | #16 | Sniffs by Kotlin-only markers (`import kotlin.`, a bare `println(` at the start of a line, `fun main(`, `data class `, `companion object`) that avoid other plugins' overlapping substrings (Java's `System.out.println(` and Go's `fmt.Println(` both contain `println(` but never at the start of a line); placed just ahead of `csharp` in `CORE_PLUGINS` |
| Ruby source | `crates/plugins/ruby` | #18 | Sniffs by Ruby-only markers (a bare `end` line, `require '`/`require "`, `attr_accessor`/`attr_reader`/`attr_writer`, a bare `puts` call, `module `, a `do \|...\|` block, or a `ruby` shebang) that avoid the Python plugin's bare `def `/`class ` check, which Ruby's own `def`/`class` lines would also match; placed just ahead of `python` in `CORE_PLUGINS` so it claims Ruby files first |
| PHP source | `crates/plugins/php` | #19 | Sniffs by the `<?php`/`<?=` opening tag, the one marker unique to PHP source; ordinary PHP code also produces bare top-level `function `/`class ` lines and a bare `require '...'` line that would otherwise be claimed first by the JavaScript/Python and Ruby plugins' overlapping markers, so it is placed first in `CORE_PLUGINS`, ahead of all other source-language plugins |
| Perl source | `crates/plugins/perl` | #20 | Sniffs by a `perl` shebang, `use strict`/`use warnings` lines, a `package Name;` declaration (semicolon-terminated, distinct from the Go plugin's bare `package main`), a top-level `sub name {` declaration, a lexical `my $` variable, or the `=~` regex-binding operator — markers not used by any sibling plugin; placed ahead of `php` in `CORE_PLUGINS`. Perl and Prolog both commonly use the `.pl` extension, but this project sniffs by content only, so a future Prolog plugin must avoid these same markers or be ordered after `perl` |
| Prolog source | `crates/plugins/prolog` | #89 | Sniffs by a `swipl`/`gprolog`/`prolog` shebang, a `:-`/`?-` line opener (the directive and query-prompt prefixes), or a `) :-` rule-neck sequence — two-character markers not used by any sibling plugin, notably distinct from the Erlang plugin's single-dash `-module(` attribute forms; placed just after `perl` in `CORE_PLUGINS` per that plugin's own note, since both commonly use the `.pl` extension and this project sniffs by content only |
| Swift source | `crates/plugins/swift` | #17 | Sniffs by Swift-only markers (`import Foundation`/`import UIKit`/`import SwiftUI`, `protocol `/`extension ` declarations, `guard let `/`guard var `, `@IBOutlet`, `@escaping`, and `func` declarations with an arrow return type `) -> `) that don't overlap this project's other source-language plugins, in particular avoiding the Go plugin's bare `func ` marker by requiring the arrow; placed just ahead of `text` in `CORE_PLUGINS`, no ordering constraint against a specific sibling since it has no overlapping markers |
| Shell script | `crates/plugins/shell` | #22 | Sniffs by a shebang naming `sh`/`bash`/`zsh`/`dash`/`ksh` (directly or via `env`), or POSIX shell keywords not used by any sibling plugin (a bare `fi`/`done`/`esac`/`then` line, `elif `, a `case ... in` opener, a line ending `; then`) or `$(` command substitution; deliberately does not sniff `${...}` parameter expansion since JavaScript/TypeScript template literals use the same syntax; placed just ahead of `text` in `CORE_PLUGINS`, no ordering constraint against a specific sibling since it has no overlapping markers |
| PowerShell source | `crates/plugins/powershell` | #23 | Sniffs by a `pwsh`/`powershell` shebang (directly or via `env`), a `<#` block/help comment opener, `[CmdletBinding()]`, a `param(`/`param (` block, or the `$PSScriptRoot`/`$PSVersionTable`/`$ErrorActionPreference` automatic/preference variables — markers not used by any sibling plugin; placed just ahead of `text` in `CORE_PLUGINS`, no ordering constraint against a specific sibling since it has no overlapping markers |
| Tcl source | `crates/plugins/tcl` | #86 | Sniffs by a `tclsh`/`wish` shebang (directly or via `env`), a top-level `proc name {args} {` opener ending in a bare `{` (distinct from the Nim plugin's `proc name(...): type =`, which ends in `=`), a `namespace eval Name {` block, or a `package require Name` statement (distinct from the Go plugin's bare `package main` and the Perl plugin's semicolon-terminated `package Name;`) — markers not used by any sibling plugin; placed just ahead of `r` in `CORE_PLUGINS`, next to `shell`/`powershell` |
| R source | `crates/plugins/r` | #25 | Sniffs by an `Rscript` shebang, the `<-` assignment operator, the `%>%` pipe, or a top-level `library(` call — markers not used by any sibling plugin; placed just ahead of `text` in `CORE_PLUGINS`, no ordering constraint against a specific sibling since it has no overlapping markers. A future Haskell, OCaml, or F# plugin sniffing `<-` for a monadic bind or list-comprehension generator must avoid this same marker, or be ordered after `r` |
| Haskell source | `crates/plugins/haskell` | #26 | Sniffs by a `runghc`/`runhaskell` shebang, a `{-# LANGUAGE` pragma, an `import qualified ` statement, or a spaced ` :: ` type signature — markers not used by any sibling plugin; deliberately does not sniff the `<-` operator per the R plugin's note above, since Haskell also uses `<-` for monadic bind; placed just ahead of `text` in `CORE_PLUGINS`, no ordering constraint against a specific sibling since it has no overlapping markers |
| Scala source | `crates/plugins/scala` | #27 | Sniffs by a `scala` shebang, an `import scala.` statement, a `case class ` declaration, `extends App`, a `def main(args: Array[String]` signature, or a compound `sealed trait ` marker — markers not used by any sibling plugin; deliberately checks `sealed trait ` rather than a bare `trait ` line start, since the Rust plugin already claims that; placed just ahead of `text` in `CORE_PLUGINS`, no ordering constraint against a specific sibling since it has no overlapping markers |
| SQL source | `crates/plugins/sql` | #24 | Sniffs case-insensitively by a statement-introducing keyword at the start of a line (`SELECT`, `INSERT INTO`, `UPDATE`, `DELETE FROM`, `CREATE TABLE`/`INDEX`/`VIEW`, `ALTER TABLE`, `DROP TABLE`) or a `PRIMARY KEY`/`FOREIGN KEY` constraint anywhere — markers not used by any sibling plugin; placed just ahead of `text` in `CORE_PLUGINS`, no ordering constraint against a specific sibling since it has no overlapping markers |
| Elixir source | `crates/plugins/elixir` | #28 | Sniffs by an `elixir` shebang, a `defmodule ` declaration, `IO.puts`/`IO.inspect`, the `@moduledoc` attribute, or the `\|>` pipe operator — markers not used by any sibling plugin; deliberately does not sniff bare `end` lines, since the Ruby plugin already claims those, so this plugin is placed just ahead of `ruby` in `CORE_PLUGINS` to claim genuine Elixir files (which also close blocks with bare `end`) via one of its own stronger markers first |
| Clojure source | `crates/plugins/clojure` | #30 | Sniffs by a `clojure` shebang, or a `(ns `/`(defn `/`(defn- `/`(defmacro ` opener or a `(require '` call — parenthesized, prefix-form markers not used by any sibling plugin; placed just ahead of `text` in `CORE_PLUGINS`, no ordering constraint against a specific sibling since it has no overlapping markers |
| Scheme/Lisp source | `crates/plugins/scheme` | #85 | Sniffs by a `(define `/`(defun `/`(lambda ` opener or a `#lang ` header — parenthesized, prefix-form markers not used by any sibling plugin (Clojure's equivalent function form is the distinct `(defn `); placed just ahead of `text` in `CORE_PLUGINS`, no ordering constraint against a specific sibling since it has no overlapping markers |
| Dart source | `crates/plugins/dart` | #31 | Sniffs by an `import 'package:`/`import 'dart:` scheme, a `library `/`part of ` directive, the lowercase `@override` annotation, or an `extends StatelessWidget`/`extends StatefulWidget` declaration — markers not used by any sibling plugin (Java's equivalent annotation is the distinct, case-sensitive `@Override`); placed just ahead of `text` in `CORE_PLUGINS`, no ordering constraint against a specific sibling since it has no overlapping markers |
| Erlang source | `crates/plugins/erlang` | #29 | Sniffs by an `escript` shebang, or the `-module(`/`-export(`/`-record(`/`-behaviour(`/`-behavior(` attribute forms, the `-spec ` type-signature attribute, or the `io:format(` qualified call — markers not used by any sibling plugin; placed just ahead of `text` in `CORE_PLUGINS`, no ordering constraint against a specific sibling since it has no overlapping markers |
| Objective-C source | `crates/plugins/objective-c` | #32 | Sniffs by the `#import <`/`#import "` header directive, an `@interface `/`@implementation `/`@protocol ` compiler directive, `@property`, `NSLog(`, or an `@"` string literal opener — markers not used by any sibling plugin. Objective-C source commonly also contains a bare `int main(` (the C plugin's marker) and Objective-C++ source commonly contains a bare `class ` declaration (the C++ plugin's marker), so this plugin is placed just ahead of `cpp` (and thus `c`) in `CORE_PLUGINS`, claiming such files by its own stronger markers first |
| Julia source | `crates/plugins/julia` | #34 | Sniffs by a `julia` shebang, a bare `using ` import line, the `import Base:` method-extension form, or a `Vector{`/`Dict{`/`Array{` curly-brace parametric type instantiation — markers not used by any sibling plugin. A bare `using ` line is also produced by a C# `using System;` line, so this plugin is placed after `csharp` in `CORE_PLUGINS`, where genuine C# files are already claimed by their own stronger markers first |
| Fortran source | `crates/plugins/fortran` | #35 | Sniffs by the `implicit none` statement, a top-level `program `/`subroutine ` declaration, a top-level `end program`/`end subroutine`/`end function`/`end module` closer, or a `write(*,*)` formatted write to standard output — markers not used by any sibling plugin. Deliberately does not sniff the spaced `::` type-declaration operator, since the Haskell plugin already claims that, nor a bare `module `/`end` line, since the Ruby plugin already claims those; placed just ahead of `text` in `CORE_PLUGINS`, no ordering constraint against a specific sibling since it has no overlapping markers |
| Assembly source | `crates/plugins/assembly` | #36 | Sniffs by a `.global`/`.globl` exported-symbol directive, a `section .text`/`.data`/`.bss` directive (with or without a leading dot, covering both GAS and NASM), an `int 0x80` software-interrupt syscall, a bare `syscall` instruction line, or an AT&T-syntax register operand (`%eax`, `%rdi`, ...) — markers not used by any sibling plugin; placed just ahead of `text` in `CORE_PLUGINS`, no ordering constraint against a specific sibling since it has no overlapping markers |
| Vim script | `crates/plugins/vimscript` | #37 | Sniffs by a `vim9script` header, the `function!`/`endfunction` declaration pair, an `autocmd` handler, a `noremap` key mapping, a `colorscheme` switch, `set nocompatible`, `syntax on`/`syntax enable`, `filetype plugin indent on`, or a `let g:` global-variable assignment — markers not used by any sibling plugin; placed just ahead of `text` in `CORE_PLUGINS`, no ordering constraint against a specific sibling since it has no overlapping markers |
| Dockerfile | `crates/plugins/dockerfile` | #39 | The issue asked to "sniff by filename" for the bare `Dockerfile` case, which this project's architecture cannot honor (`PluginCore::sniff` only ever receives a content prefix, never a path — see #38's unanswered comments for the same conflict on the Makefile plugin). Substituted a content-based sniff instead, since Dockerfile instructions are reliable content markers unlike Makefile's: a `# syntax=` parser directive, or an instruction keyword (`FROM `, `RUN `, `COPY `, `ADD `, `WORKDIR `, `EXPOSE `, `ENV `, `ARG `, `USER `, `LABEL `, `VOLUME `, `ENTRYPOINT `, `CMD `, `ONBUILD `, `HEALTHCHECK `, `STOPSIGNAL `, `SHELL `, `MAINTAINER `) at the start of a line, matched case-sensitively in the uppercase style Dockerfiles conventionally use so plain English prose beginning "From ..." doesn't false-positive. Placed just ahead of `shell` in `CORE_PLUGINS`, since a `RUN` instruction using `$(...)` command substitution or an embedded multi-line shell conditional (bare `fi`/`done`/`esac`) would otherwise be misclaimed by the shell plugin's markers first |
| GraphQL schema | `crates/plugins/graphql` | #40 | Sniffs by a top-level `type `/`input ` object declaration opened with `{`, a `schema {` definition, a top-level `scalar `/`extend type ` declaration, or a `directive @` usage — markers not used by any sibling plugin. Deliberately does not sniff a bare `interface `/`enum ` line, since the TypeScript plugin already claims those; placed just ahead of `text` in `CORE_PLUGINS`, no ordering constraint against a specific sibling since it has no overlapping markers |
| Makefile | `crates/plugins/makefile` | #38 | Sniffs by a `.PHONY:` declaration, a `$(MAKE)` recursive invocation, a reference to one of Make's implicit built-in variables (`$(CC)`, `$(CXX)`, `$(CFLAGS)`, `$(CXXFLAGS)`, `$(LDFLAGS)`, `$(RM)`, `$(AR)`), a GNU Make conditional (`ifeq (`, `ifneq (`, `ifdef `, `ifndef `), or a recipe line (starting with `@`, `-`, or containing a `$(...)` reference) immediately following a `target: prerequisites` rule header. The issue asked to "sniff by filename, not extension" for the bare `Makefile`/`makefile`/`GNUmakefile` case, but `PluginCore::sniff` only ever receives a content prefix (`crates/plugin-api/src/lib.rs`) — no path reaches it, and threading one through would mean changing the trait and every existing plugin, well outside this issue's single-plugin scope. Issue #38's own comment thread raised this conflict three times without a resolution; this PR proceeds on content-sniffing, the same substitution already used for the sibling Dockerfile issue (#39, PR #132). The most common Makefile idiom, `NAME := value`, is also the Go plugin's bare `:=` marker, and `NAME := value`/`$(VAR)` recipes are also caught by the shell plugin's bare `$(` marker, so this plugin is placed just ahead of `go` in `CORE_PLUGINS` (and thus ahead of `shell`, `java`, and every other plugin after `go`) so real Makefiles are claimed by their own stronger markers first. The recipe-line-after-target-header check deliberately requires an `@`/`-` prefix or a `$(...)` reference (rather than any tab-indented line) so a real assembly file's tab-indented instructions under a `label:` line, which the `assembly` plugin already claims, are not misclaimed — covered by a unit test |
| Solidity | `crates/plugins/solidity` | #42 | Sniffs by a `pragma solidity` version directive, an `SPDX-License-Identifier:` header, a top-level `contract`/`interface`/`library` declaration, a `mapping(` type, an `emit ` event, a `modifier ` declaration, or the `msg.sender`/`msg.value` built-ins — markers not used by any sibling plugin (deliberately avoids the JavaScript plugin's `require(` marker and the TypeScript plugin's `interface`/`public `/`private ` markers, since Solidity's own `pragma`/`contract`/`mapping(` markers are stronger and unambiguous); placed just ahead of `text` in `CORE_PLUGINS`, no ordering constraint against a specific sibling since it has no overlapping markers |
| HTML | `crates/plugins/html` | #44 | Sniffs case-insensitively by a `<!doctype html`/`<html`/`</html>` document marker, a `<head>`/`</head>`/`<body>`/`</body>` structural tag, or a `<title>` tag — markers not used by any sibling plugin. Deliberately does not sniff bare `<div`/`<script`/`<style` tags, since those are common enough in other contexts (e.g. a JavaScript template literal) to false-positive; a future XML or SVG plugin sniffing the same document-structure tags this plugin does will need its own stronger markers, or to be ordered after `html`; placed just ahead of `text` in `CORE_PLUGINS`, no ordering constraint against a specific sibling since it has no overlapping markers |
| XML | `crates/plugins/xml` | #46 | Sniffs case-insensitively by an `<?xml` declaration, a `<![CDATA[` section, or an `xmlns=`/`xmlns:` namespace attribute — markers not used by any sibling plugin. Deliberately does not sniff bare document-structure tags, per the HTML plugin's own note above; placed just after `html` in `CORE_PLUGINS` so a real HTML document (which never carries these XML-specific markers) is claimed first |
| reStructuredText | `crates/plugins/restructuredtext` | #48 | Sniffs by a `.. name::` directive marker, a `.. _label:` hyperlink target, or a section-title line immediately followed by a matching punctuation-only underline at least as long as the title — markers not used by any sibling plugin. Deliberately does not sniff a bare underline line on its own, since a future Markdown plugin's setext headers (`===`/`---` under a title line) use the same adornment characters; that plugin would need its own stronger markers, or to be ordered against this one if the ambiguity can't otherwise be resolved; placed just ahead of `text` in `CORE_PLUGINS`, no ordering constraint against a specific sibling since it has no overlapping markers today |
| JSON | `crates/plugins/json` | #50 | Sniffs by JSON's own grammar rather than a fixed marker: the prefix must start with `{`/`[`, and the first value in it must parse cleanly or run out of bytes without a syntax error (since `sniff` only sees a bounded prefix, a large file's JSON is expected to still be mid-value at that point) — no sibling plugin's markers overlap with this. Presents a real tree view (one indented line per object key or array index, recursing into children), not just the raw source, per the issue's request that it be meaningfully distinct from the `text` plugin's presentation; falls back to raw content if the file doesn't parse as JSON. Placed just ahead of `text` in `CORE_PLUGINS`. A future YAML plugin, whose flow style is a JSON superset, will need to be ordered after this one, or use its own stronger markers, to claim genuine YAML documents that happen to open with `{` or `[` |
| TOML | `crates/plugins/toml` | #52 | Sniffs by a table header line (`[section]`/`[[array-of-tables]]`) or a `key = value` line whose value opens with a strong TOML-only literal (a quoted string, `true`/`false`, or a bare ISO 8601 date/date-time) — deliberately excludes bare numbers and inline arrays/tables from the value check, since a bare `key = [...]`/`key = 1` line is common top-level assignment syntax this project's Python/JavaScript plugins don't themselves claim but could otherwise false-positive on. Parses with the `toml` crate and converts to a JSON value so it can reuse JSON's tree-view presentation shape; falls back to raw content if the file doesn't parse as TOML. Placed just ahead of `text` in `CORE_PLUGINS`, right after `json` (no ordering dependency, kept adjacent since both are structured-data formats). A future INI/properties plugin sniffing the same `[section]` header syntax will need to be ordered after this one, or use its own stronger markers |
| CSV/TSV | `crates/plugins/csv` | #54 | Sniffs structurally rather than by a fixed marker: at least two records (read with the `csv` crate) share the same field count, and that count is more than one — comma and tab are both tried, since sniffing has no access to the file's extension. This is a weaker signal than this project's other structural checks (JSON/TOML) and can false-positive on other content that happens to have consistent per-line delimiter counts; accepted as an inherent limitation of content-only sniffing for a format with no reserved syntax. `view` picks its delimiter from the `.csv`/`.tsv` extension when present, falling back to the same tab/comma sniff otherwise, then parses with the `csv` crate. Presents an aligned, spreadsheet-like grid (padded columns and a header rule) per the issue's requirement that it be more than syntax-highlighted source; falls back to raw content if any row fails to parse. Placed just ahead of `text` in `CORE_PLUGINS`, right after `toml` (no ordering dependency, kept adjacent since both are structured-data formats) |
| Parquet | `crates/plugins/parquet` | #55 | Sniffs by the `PAR1` magic number Parquet repeats at both ends of the file — `sniff` only ever sees a bounded prefix from the start, but the leading copy alone is a marker not used by any sibling plugin. Reads the footer metadata and up to 200 rows with the `parquet` crate's low-level, non-`arrow` API (`SerializedFileReader` plus the record `Row`/`Field` types), keeping this plugin's dependency tree to `parquet` itself plus its common compression codecs rather than pulling in the `arrow` columnar library. Presents the same aligned, spreadsheet-like grid shape as `csv`, implemented independently. Since the magic number is a strong, unambiguous marker, `view` surfaces a genuine parse failure as an error rather than falling back to raw content, matching `archive`/`pdf`'s pattern for other strongly-sniffed binary formats. Placed after `pdf` at the end of `CORE_PLUGINS`, alongside this project's other binary/magic-number-sniffed plugins |
| Apache Avro | `crates/plugins/avro` | #56 | Sniffs by the `Obj\x01` magic number that opens every Avro object container file — a marker not used by any sibling plugin. Reads every record with the `apache-avro` crate's `Reader`, deriving column headers from the writer schema's top-level record fields (falling back to a single `value` column for a non-record schema, since Avro allows any type at the top level) and rendering each Avro `Value` variant to display text with a small recursive formatter, since `Value` has no `Display` impl of its own. Presents the same aligned, spreadsheet-like grid shape as `csv`/`parquet`. Since the magic number is a strong, unambiguous marker, `view` surfaces a genuine parse failure as an error rather than falling back to raw content, matching `parquet`'s pattern. Placed after `parquet` at the end of `CORE_PLUGINS`, alongside this project's other binary/magic-number-sniffed plugins |
| MessagePack | `crates/plugins/msgpack` | #57 | MessagePack has no reserved magic number, and a bare scalar's leading byte overlaps too much of arbitrary binary data to be a useful marker, so `sniff` only recognises a map or array: its first byte must be a map/array format code, and decoding it with the `rmpv` crate must either succeed as a `Map`/`Array` or run out of bytes doing so (expected when the value is bigger than the bounded prefix `sniff` sees, mirroring the JSON plugin's own EOF-tolerant check). `view` decodes the file's leading value with `rmpv` and converts it to a small tree type kept independent of `serde_json::Value` (rather than reusing the TOML plugin's conversion), since MessagePack's map keys may be any value, not just strings, and its integers span the full `u64` range, which JSON's own map/number types can't represent losslessly. Presents the same indented tree shape as `json`/`toml`, implemented independently. Placed just ahead of `text` in `CORE_PLUGINS`, right after `csv` (no ordering dependency, kept adjacent since all are structured-data formats) |
| SQLite database | `crates/plugins/sqlite` | #58 | Sniffs by the fixed 16-byte `SQLite format 3\0` header every SQLite file opens with — a marker not used by any sibling plugin. Reads every user table (via `sqlite_master`, excluding SQLite's own `sqlite_%` bookkeeping tables) with the `rusqlite` crate, built with its `bundled` feature so this plugin links a compiled-from-source SQLite rather than depending on one being present on the host; opens the database read-only (`SQLITE_OPEN_READ_ONLY`) since a plugin's `view` only ever inspects a file. Presents each table as its own aligned, spreadsheet-like grid (same shape as `csv`/`parquet`/`avro`, implemented independently), stacked one after another with a row count and truncation note per table, since the issue asked for a schema-plus-rows table browser rather than a single flat grid. Since the magic header is a strong, unambiguous marker, `view` surfaces a genuine read failure as an error rather than falling back to raw content, matching `parquet`/`avro`'s pattern for other strongly-sniffed binary formats. Placed after `avro` at the end of `CORE_PLUGINS`, alongside this project's other binary/magic-number-sniffed plugins |
| Jupyter Notebook | `crates/plugins/jupyter-notebook` | #59 | A notebook file is itself JSON, so `sniff` can't rely on JSON's own grammar the way the `json` plugin does; instead it requires the JSON object to carry both a `cells` key and either an `nbformat` or a `cell_type` key — markers not used by any sibling plugin and specific enough not to false-positive on an arbitrary JSON document. Since a genuine notebook would otherwise also match `json`'s more general JSON-grammar check, this plugin is placed just ahead of `json` in `CORE_PLUGINS` so its own stronger markers claim the file first. Reads and parses the full file (mirroring `json`'s pattern of not truncating before parsing), rendering each cell (code, markdown, or raw) as a `[N] <type> cell` header, its joined source lines, and, for a code cell, its `stream`/`execute_result`/`display_data`/`error` outputs rendered to display text (a non-text-plain output such as an image is shown as an `<mime type> output` placeholder rather than embedding its raw data) — per the issue's requirement to render cells rather than raw JSON. Falls back to raw content if the file doesn't parse as JSON or has no top-level `cells` array |
| Word processing document | `crates/plugins/word-document` | #60 | Covers `.docx`, `.odt`, and `.rtf` as one plugin, per the issue's direction that this document family is one paginated word-processing view rather than a plugin per container format. RTF is sniffed by its fixed `{\rtf1` opening control word. DOCX and ODT are both ZIP containers sharing the `archive` plugin's own `PK\x03\x04` magic, so this plugin also requires the raw prefix to contain either `word/document.xml` (the DOCX-specific internal part path, distinct from `xl/workbook.xml`/`ppt/presentation.xml`) or `application/vnd.oasis.opendocument.text` (the ODF mimetype, spec-mandated to be the archive's first, uncompressed entry) — markers not used by any sibling plugin; placed just ahead of `archive` in `CORE_PLUGINS` so a real Word document is claimed first. A ZIP entry's filename is always stored uncompressed in its local file header regardless of the entry's own compression, so the DOCX marker is visible in a raw prefix as long as that entry falls within the sniffed window; a document whose preceding parts push `word/document.xml` past that window will not be recognised, an accepted content-sniffing limitation shared with this project's other structurally-sniffed formats. Extracts paragraph text with a hand-rolled walk that strips XML tags and decodes the five predefined entities, splitting on `<w:p>`/`<text:p>` closing tags (both formats' own paragraph elements), and a hand-rolled RTF-to-text state machine that skips font/colour/style/info/generator/picture destination groups and turns `\par`/`\line` into paragraph breaks — no dependency beyond the `zip` crate already used by `archive`, since no well-established DOCX/ODT/RTF parsing crate matched this project's minimal-dependency pattern |
| Spreadsheet | `crates/plugins/spreadsheet` | #61 | Covers `.xlsx` and `.ods` as one plugin, per the issue's direction that this document family is one tabular-grid view rather than a plugin per container format. Both are ZIP containers sharing the `archive`/`word-document` plugins' own `PK\x03\x04` magic, so this plugin also requires the raw prefix to contain either `xl/workbook.xml` (the XLSX-specific internal part path, distinct from `word/document.xml`/`ppt/presentation.xml`) or `application/vnd.oasis.opendocument.spreadsheet` (the ODF mimetype, spec-mandated to be the archive's first, uncompressed entry) — markers not used by any sibling plugin; placed just ahead of `archive` in `CORE_PLUGINS`, right after `word-document` (no ordering dependency between the two, kept adjacent since both are ZIP-container document families), so a real spreadsheet is claimed first. Unlike `word-document`'s hand-rolled paragraph walk, correctly reconstructing a sheet's row/column grid needs OOXML's shared-string table and cell-reference indexing (XLSX) and ODF's row/column repeat-count compression (ODS), which is significantly more structural work than flattening paragraph text; this plugin instead reads both formats with the `calamine` crate (already MIT-licensed, matching this project's licence), keeping the plugin's own code to dispatch plus rendering. Reads up to 200 rows per sheet, converting every cell to display text with a small `calamine::Data` match; presents each sheet as its own aligned grid (same shape as `csv`/`parquet`/`avro`/`sqlite`, implemented independently) with a row count and truncation note, but — unlike those tabular plugins — with no header row, since a spreadsheet's first row is arbitrary user data rather than a schema |
| Presentation | `crates/plugins/presentation` | #62 | Covers `.pptx` and `.odp` as one plugin, per the issue's direction that this document family is one slide-deck view rather than a plugin per container format. Both are ZIP containers sharing the `archive`/`word-document`/`spreadsheet` plugins' own `PK\x03\x04` magic, so this plugin also requires the raw prefix to contain either `ppt/presentation.xml` (the PPTX-specific internal part path, distinct from `word/document.xml`/`xl/workbook.xml`) or `application/vnd.oasis.opendocument.presentation` (the ODF mimetype, spec-mandated to be the archive's first, uncompressed entry) — markers not used by any sibling plugin; placed just ahead of `archive` in `CORE_PLUGINS`, right after `spreadsheet` (no ordering dependency between the three, kept adjacent since all are ZIP-container document families), so a real presentation is claimed first. Reuses `word-document`'s hand-rolled XML-tag-stripping paragraph walk (this plugin's own copy, checking `<a:p>`/`<text:p>` closing tags — DrawingML's and ODF's own paragraph elements respectively) rather than a dependency, matching that plugin's precedent that no established minimal-dependency crate covers this container format. PPTX slides are separate `ppt/slides/slideN.xml` parts with no in-file ordering guarantee beyond the numeric part-name suffix this plugin sorts by (the authoritative order lives in a relationship list this plugin does not resolve) — an accepted limitation shared with this project's other structurally-sniffed formats; ODP slides are `<draw:page>` elements inside one `content.xml`, in true presentation order already. Presents each slide under a `Slide N` header with its extracted text lines, or `(no text)` if a slide has none |
| EPUB | `crates/plugins/epub` | #63 | `.epub` is a ZIP container carrying the format's own `application/epub+zip` mimetype as its first, uncompressed entry per the OCF spec, so this plugin sniffs the shared `archive`-family `PK\x03\x04` magic plus that mimetype in the raw prefix — a marker not used by any sibling plugin — and is placed just ahead of `archive` in `CORE_PLUGINS`, right after `presentation` (no ordering dependency, kept adjacent as another ZIP-container document family). Reads the book the way the OCF/OPF specs lay it out rather than as one fixed part path: follows `META-INF/container.xml`'s `rootfile` to the package document, reads `dc:title`/`dc:creator` from its metadata, then resolves the manifest and spine to read each chapter's XHTML content document in reading order — a hand-rolled walk with the same tag-stripping/entity-decoding shape as `word-document`/`presentation`, splitting on `<p>` closing tags rather than a namespaced paragraph element, since EPUB content documents are plain XHTML. A manifest href that only resolves outside the package document's own directory, or a spine `idref` with no matching manifest item, is skipped rather than resolved, an accepted limitation shared with this project's other structurally-read formats. Presents the title and author (if present) followed by each chapter under a `Chapter N` header, or `(no text)` if a chapter has none |
| Comic book archive | `crates/plugins/comic-archive` | #64 | Covers `.cbz` and `.cbr` as one plugin: both are a bare ZIP or RAR archive of page images with no manifest, so `sniff` recognises RAR by the `rars` crate's own `detect_archive_family` signature check (no other plugin sniffs RAR, so this needs no extra marker), and ZIP by the shared `archive`-family `PK\x03\x04` magic plus a recognised image extension (`.jpg`/`.jpeg`/`.png`/`.gif`/`.bmp`/`.webp`) found in the raw prefix — a real comic's first local file header is a page image, so its name sits inside the sniffed window, distinct from every other ZIP-container plugin's own fixed part-name/mimetype marker. Placed just ahead of `archive` in `CORE_PLUGINS`, right after `epub` (no ordering dependency, kept adjacent as another ZIP-container format), so a genuine comic archive is claimed before the generic archive listing. Reads ZIP entries with the `zip` crate already used by `archive`/`epub`, and RAR entries with the `rars` crate (MIT OR Apache-2.0, pure Rust, no `unsafe`) — the only actively maintained RAR reader that doesn't require linking the non-free `unrar` library. Filters both to image-extension entries only and sorts by name for reading order, since neither format's directory order is guaranteed to match natural page order; a page numbered without zero-padding (`page2.jpg` sorting after `page10.jpg`) is an accepted limitation of name-order sorting shared with `presentation`'s own PPTX slide-ordering approach. Presents a `Page N: name (size bytes)` line per page instead of `archive`'s flat entry listing, per the issue's direction that this be a page-by-page reader |
| Audio | `crates/plugins/audio` | #65 | Covers `.mp3`/`.flac`/`.wav`/`.ogg`/`.m4a` as one plugin, matching the issue's direction that one plugin cover common audio codecs the way `image` covers multiple raster codecs. `sniff` matches each container's own fixed magic — an `ID3` tag header or a bare MPEG frame sync (`0xFF` plus three set high bits) for MP3, `fLaC` for FLAC, a RIFF/WAVE header for WAV, `OggS` for Ogg, and an `ftyp` box at offset 4 for M4A — markers not used by any sibling plugin, so placement in `CORE_PLUGINS` has no ordering dependency; kept adjacent to `comic-archive`/`epub` as this project's other recently-added binary formats. Reads properties and the primary tag (falling back to the first tag present) with the `lofty` crate (MIT OR Apache-2.0, pure Rust, no `unsafe`) rather than a hand-rolled parser, since correctly reading five different container/tag formats' framing is significantly more structural work than any of this project's own hand-rolled text-extraction plugins. The view is metadata only (format, duration, sample rate, channels, bitrate, title/artist/album) rather than decoded audio or a waveform, matching `image`'s own precedent that the presentation half is lines of text, not toolkit-specific playback data |
| Video | `crates/plugins/video` | #66 | Covers `.mp4`/`.mkv`/`.webm`/`.avi` as one plugin, matching the issue's direction that one plugin cover common video codecs the way `image` covers multiple raster codecs. MP4 is sniffed by an `ftyp` box whose major brand is a curated, video-specific brand (`isom`/`mp42`/`avc1`/... ), deliberately excluding the audio-only `M4A `/`M4B `/`M4P ` brands `audio`'s own brand-agnostic `ftyp` check already claims — this plugin is placed just ahead of `audio` in `CORE_PLUGINS`/`PRESENTATION_PLUGINS` so a real video `.mp4` is claimed first, leaving `audio`'s looser check to still catch real `.m4a` files. Matroska and WebM share the EBML header magic (`\x1A\x45\xDF\xA3`) — a marker not used by any sibling plugin — and, since the `matroska` crate exposes no DocType accessor, `view` distinguishes WebM from plain Matroska by scanning the sniffed prefix for the literal `webm` DocType string every WebM file's EBML header carries near its start, a marker-in-raw-prefix approach matching this project's other structurally-sniffed formats. AVI is sniffed by a RIFF/AVI header, distinct from `audio`'s own RIFF/WAVE check. Reads MP4 with the `mp4` crate and Matroska/WebM with the `matroska` crate (both MIT-licensed, pure Rust, no `unsafe`); AVI has no comparable minimal-dependency crate, so `view` hand-parses the RIFF `avih` main header chunk (nested inside the `hdrl` `LIST`) for resolution and duration, following `word-document`'s precedent of a hand-rolled reader for a less-common container. The view is metadata only (format, duration, resolution, video/audio codec) rather than a decoded thumbnail or playback frames, matching `audio`'s own precedent that the presentation half is lines of text, not toolkit-specific playback data |
| SVG | `crates/plugins/svg` | #67 | Sniffs case-insensitively by an `<svg`/`</svg>` tag boundary or the SVG namespace URI (`xmlns="http://www.w3.org/2000/svg"`) — markers specific enough not to overlap with any sibling plugin's, but a bare `<svg` tag is also generic XML/HTML document-structure syntax, so this plugin is placed just ahead of `html`/`xml` in `CORE_PLUGINS`/`PRESENTATION_PLUGINS`, per those plugins' own notes, so a real SVG file is claimed before either's own looser markers. The view hand-parses the first `<svg ...>` tag's `width`/`height` attributes (stripping a CSS unit suffix such as `px`), falling back to the third/fourth numbers of its `viewBox` attribute when explicit dimensions are absent, rather than the raw-content dump `xml`/`html` present — matching the issue's direction that this render as a vector graphic, distinct from the raster `image` plugin's own metadata-only precedent (format, dimensions, file size on disk) despite being XML text under the hood |
| Font | `crates/plugins/font` | #68 | Covers `.ttf`/`.otf`/`.woff`/`.woff2` as one plugin, matching the issue's direction that one plugin cover common font container formats the way `image` covers multiple raster codecs. `sniff` matches each container's own fixed `sfnt`/wrapper magic — `0x00010000` or `true` for TrueType, `OTTO` for OpenType, `wOFF` for WOFF, `wOF2` for WOFF2 — markers not used by any sibling plugin, so placement in `CORE_PLUGINS` has no ordering dependency; kept adjacent to `image` as another binary, metadata-only format. Reads the `sfnt` table directory with the `ttf-parser` crate (MIT OR Apache-2.0, pure Rust, no `unsafe`), the de facto standard minimal-dependency font parser; `ttf-parser` doesn't itself decompress the WOFF/WOFF2 wrapper, so a WOFF or WOFF2 file is first unwrapped to its underlying `sfnt` bytes with the `wuff` crate (MIT, pure Rust, no `unsafe`) before parsing. The view is metadata only (container format, family/subfamily from the `name` table, units per em, glyph count, monospace flag) rather than a rendered glyph preview, matching `image`'s own precedent that the presentation half is lines of text, not toolkit-specific outline data |
| 3D model | `crates/plugins/model3d` | #69 | Covers `.obj`/`.stl`/`.gltf` as one plugin, matching the issue's direction that one plugin cover common 3D model formats the way `image` covers multiple raster codecs. None of the three share magic bytes or even a format family (OBJ and STL are raw text/binary geometry, glTF is a JSON scene graph), so `sniff` is content-only per format: OBJ by a line opening with an OBJ-only directive (`vt`/`vn`/`vp`/`usemtl`/`mtllib`) or both a vertex (`v`) and a face (`f`) line together; ASCII STL by its `solid` opener plus a `facet normal` directive (a bare `solid`-prefixed word is not enough, since that's also plain English); binary STL by its 80-byte header followed by a triangle count bounded to a plausible maximum, since that variant has no true magic number (an accepted content-sniffing limitation shared with this project's other structurally-sniffed formats); glTF by its JSON carrying the spec-mandated `asset` object with `version`, a combination not used by this project's other JSON-based formats — placed just ahead of `json` in `CORE_PLUGINS` so a real glTF document (which is otherwise valid JSON) is claimed first. Hand-rolled parsing throughout (no dependency beyond `serde_json`, already used for the glTF half): OBJ counts `v `/`f ` lines, ASCII STL counts `facet normal` occurrences, binary STL reads its header's triangle count, and glTF reads `meshes`/`scenes` array lengths and `asset.generator`. The view carries the union of each format's own fields rather than one shared geometry model, since OBJ/STL expose raw vertex/face counts while glTF exposes mesh/scene counts instead |
| Disk image | `crates/plugins/disk-image` | #70 | Covers `.iso` (ISO 9660) only — see Rejected below for why `.dmg` isn't part of this plugin. ISO 9660's `CD001` standard identifier sits inside the Primary Volume Descriptor at byte offset 32769 (sector 16), far past every other sniffed format's near-offset-0 magic, so this plugin required raising `crates/service`'s `SNIFF_PREFIX_LEN` from 512 to 32774 bytes — a project-wide sniff-window change no earlier plugin needed, since it's read on every sniff call regardless of which plugin ultimately matches. Hand-rolled parsing (no dependency: the Primary Volume Descriptor is a simple fixed-layout binary struct), reading the descriptor's Volume Identifier (label), Logical Block Size, and Volume Space Size (block count) fields directly from the file at their fixed offsets, following `model3d`/`video`'s precedent of hand-parsing a structurally simple binary format rather than adding a dependency. The view is metadata only (format, volume label, block size/count, computed volume size, file size), matching `font`/`model3d`'s own precedent that the presentation half is lines of text |
| OS package | `crates/plugins/package-archive` | #71 | Covers `.deb` (Debian binary package) and `.rpm` (RPM package) as one plugin, matching the issue's direction that this be one "OS package" view rather than a plugin per package format. `.deb` is a plain Unix `ar` archive, sniffed by the format's fixed `!<arch>\n` global header plus a `debian-binary` first member name (a marker not used by any sibling plugin, since no other plugin sniffs `ar`); hand-rolled with the `ar` and `tar` crates plus `flate2`/`lzma-rs` for the gzip/xz member compression real `.deb` files use to read the `control.tar.*` member's metadata (name, version, architecture, dependencies) and the `data.tar.*` member's file list — a zstd-compressed member is not decoded, an accepted content-sniffing limitation shared with this project's other structurally-sniffed formats. `.rpm` is sniffed by its lead's fixed 4-byte magic number and read entirely with the `rpm` crate (built with `default-features = false`, adding no compression/signing dependencies to the production build), whose header tags carry the same metadata, dependencies, and file list without needing to touch the compressed cpio payload at all; the crate's `payload`/`gzip-compression` features are enabled only under `[dev-dependencies]`, so the plugin's own tests can build a real `.rpm` fixture with `rpm::PackageBuilder` without that capability shipping in `service`/`tui`/`gui`. Presents package metadata and dependencies alongside the file list, distinct from the generic `archive` plugin's flat listing, per the issue's own direction |
| Certificate/key | `crates/plugins/certificate` | #72 | Covers `.pem`/`.crt`/`.cer`/`.key` as one plugin: X.509 certificates, PKCS#10 certificate signing requests, and private/public keys all share the same PEM text envelope. `sniff` recognises only that envelope — a `-----BEGIN CERTIFICATE`/`PRIVATE KEY-----`/`PUBLIC KEY-----` marker not used by any sibling plugin — and deliberately does not attempt bare DER, since a DER file's leading `SEQUENCE` tag byte has no signal strong enough to serve as a marker; placed just ahead of `text` in `CORE_PLUGINS`, right after `msgpack` (no ordering dependency, kept adjacent to this project's other structured-data formats), since PEM is itself valid UTF-8 that `text`'s own catch-all sniff would otherwise claim first. Decodes every PEM block in the file (a chain or a cert-plus-key file yields more than one) with the `pem` crate, then dispatches by each block's own label: `x509-parser` reads a `CERTIFICATE` block's subject/issuer/serial/validity and a `CERTIFICATE REQUEST` block's subject; a generic PKCS#8 `PRIVATE KEY` or SPKI `PUBLIC KEY` block's algorithm is read with the `pkcs8`/`spki`/`der` crates by matching its OID (RSA, EC — with curve name from the algorithm parameters where present, Ed25519, Ed448, X25519, X448, DSA), while the three legacy, non-PKCS#8 labels (`RSA`/`EC`/`DSA PRIVATE KEY`) are named directly from their own label rather than parsed, since OpenSSL's older PKCS#1/SEC1 formats carry no more algorithm information than that. A block that fails to decode as its label promises is reported as an unrecognized block rather than failing the whole file, since one bad block (or a passphrase-encrypted key this plugin doesn't attempt to decrypt) shouldn't hide the rest of a chain |
| Executable/object | `crates/plugins/executable` | #73 | Covers `.exe`/`.dll` (PE/COFF), `.so` (ELF), and `.dylib` (Mach-O, thin or universal) as one plugin, per the issue. `sniff` matches each container's own fixed magic directly — ELF's `\x7fELF`, PE/COFF's `MZ` DOS stub, and Mach-O's thin 32/64-bit or fat magic in either byte order — markers not used by any sibling plugin, so placement in `CORE_PLUGINS` has no ordering dependency; kept adjacent to `font` as another binary, metadata-only format. Parses the whole file with the `object` crate (MIT OR Apache-2.0, pure Rust, no `unsafe`), the de facto standard unified reader across these container formats, rather than one parser per format; reads the format, architecture, bitness, and entry point from its header, and lists section names/sizes and named symbols (each capped at `MAX_ENTRIES`, matching `archive`'s own truncation limit). The view is headers/sections/symbols, not a raw hex dump, per the issue, matching `font`'s own precedent that the presentation half is lines of text |
| WebAssembly module | `crates/plugins/wasm` | #74 | `.wasm` opens with a fixed 8-byte header (the `\0asm` magic followed by a `u32` version) — a marker not used by any sibling plugin, so placement in `CORE_PLUGINS` has no ordering dependency; kept adjacent to `executable`/`font` as another binary, metadata-only format. Parses the whole module structurally with the `wasmparser` crate's low-level `Parser`/`Payload` walk (default features disabled bar `std`, since a metadata view needs the module's shape, not the `validate`/`component-model`/`simd` features' full type-checking), reading its imports, exports, and per-kind counts (functions/memories/tables/globals, each including both imported and defined items) — each import/export list capped at `MAX_ENTRIES`, matching `executable`'s own truncation limit. The view is a summary plus import/export lists, not a disassembly, matching `font`/`executable`'s own precedent that the presentation half is lines of text |
| HDF5 | `crates/plugins/hdf5` | #75 | Sniffs by the fixed 8-byte `\x89HDF\r\n\x1a\n` superblock signature every HDF5 file opens with — a marker not used by any sibling plugin, so placement in `CORE_PLUGINS` has no ordering dependency; kept adjacent to `sqlite` as another structured, self-describing binary container. Reads with the `hdf5-metno` crate (the maintained fork of the archived `hdf5` crate) built with its `static` feature, which compiles the HDF5 C library from source via `cmake` rather than depending on one being present on the host, matching `sqlite`'s own `rusqlite`/`bundled` precedent of vendoring a build over requiring a system library; this plugin's own code stays FFI-free and `unsafe`-free, since only the dependency's internals cross into C. `view` walks the group tree recursively from the root with `Group::groups`/`Group::datasets`, collecting every nested group's full path and every dataset's full path, shape, and element type (`Datatype::to_descriptor`'s `Debug` rendering, e.g. `Integer(4)`). Presents the dataset/group hierarchy as a flat, path-sorted list (`/group/`, `/group/dataset [2×2] Float(8)`) rather than falling back to hex, per the issue — the full HDF5 path already conveys nesting, so no separate indented-tree renderer was needed |
| GeoJSON | `crates/plugins/geojson` | #76 | `.geojson` is itself JSON, so `sniff` can't rely on JSON's own grammar the way the `json` plugin does; instead it requires a `"type"` key, one of RFC 7946's own object-type values (`FeatureCollection`, `Feature`, `Point`, ...) as a quoted value, and one of `coordinates`/`features`/`geometries` — markers not used by any sibling plugin and specific enough not to false-positive on an arbitrary JSON document carrying an unrelated `type` field. Since a genuine GeoJSON document would otherwise also match `json`'s more general JSON-grammar check, this plugin is placed just ahead of `json` in `CORE_PLUGINS`, right after `model3d` (no ordering dependency, kept adjacent as another structured, coordinate-bearing format), so its own stronger markers claim the file first. Hand-rolled parsing with `serde_json::Value` (no dependency beyond what `json`/`toml`/`msgpack` already use), recursively walking `FeatureCollection`/`Feature`/`GeometryCollection` wrappers down to each geometry's `coordinates` array to collect every coordinate pair and compute a bounding box. Renders an ASCII map — coordinate points plotted on a fixed-size character grid scaled to the document's bounding box — per the issue's direction that this be distinct from the generic `json` plugin's indented tree view despite being JSON under the hood, alongside a feature list labelled by each feature's `name`/`title`/`id` property where present |
| Photoshop document | `crates/plugins/psd` | #77 | `sniff` matches the fixed `8BPS` signature every PSD/PSB file opens with — a marker not used by any sibling plugin, so placement in `CORE_PLUGINS` has no ordering dependency; kept adjacent to `image` as another binary raster format. Parses with the `psd` crate (MIT OR Apache-2.0, pure Rust, no `unsafe`), reading the canvas dimensions and each layer's name in stacking order. Presents a layer panel — a dimension line, a layer count, and one `- <name>` line per layer — per the issue's direction that this be distinct from the flat raster metadata view `image` already provides |
| F# source | `crates/plugins/fsharp` | #78 | Sniffs by a `dotnet fsi`/`fsharpi` F# Interactive shebang, the `[<EntryPoint>]` attribute, or the `printfn ` formatted-print function — markers not used by any sibling plugin. Deliberately does not sniff `<-` (F#'s mutable-assignment operator) or a bare `let rec `, since the R plugin's own note above already claims `<-` and a future OCaml plugin would also use `let rec `; placed just after `haskell` in `CORE_PLUGINS`, no ordering constraint against a specific sibling since it has no overlapping markers |
| OCaml source | `crates/plugins/ocaml` | #79 | Sniffs by an `ocaml`/`ocamlrun` shebang, the qualified `Printf.printf ` call (distinct from F#'s unqualified `printfn ` and C's unqualified `printf(`), the `;;` top-level phrase terminator, a `module type ` signature declaration, or a bare `let rec ` line — the last claimed per the F# plugin's own note above that it deliberately left that marker unclaimed for this plugin; placed just after `fsharp` in `CORE_PLUGINS`, no ordering constraint against a specific sibling since it has no overlapping markers |
| Groovy source | `crates/plugins/groovy` | #80 | Sniffs by a `groovy` shebang, `import groovy.`/`@groovy.transform.`/`@Grab(` (mirroring the Java/Kotlin/Scala plugins' own `import java.`/`import kotlin.`/`import scala.` checks), a brace-bodied `def` method declaration (ending the line with `{`, unlike Python's colon-terminated `def greet():` or Ruby's brace-less `def`/`end` pairing), or a bare `println ` call (a space rather than an opening paren, distinct from Kotlin's own `println(` marker) — placed just after `kotlin` in `CORE_PLUGINS` as another JVM-hosted language, no ordering constraint against a specific sibling since it has no overlapping markers |
| Nim source | `crates/plugins/nim` | #81 | Sniffs by a `nim` shebang (e.g. `#!/usr/bin/env -S nim r`), an `import std/` standard-library import, a `{.` compiler-pragma opener (e.g. `{.push checks: off.}`), or a top-level `proc` declaration ending its line with `=` (Nim's procedure syntax, e.g. `proc greet(name: string): string =`) — markers not used by any sibling plugin; placed just after `ocaml` in `CORE_PLUGINS`, no ordering constraint against a specific sibling since it has no overlapping markers |
| Elm source | `crates/plugins/elm` | #82 | Sniffs by `type alias ` (Elm's own vocabulary for a type synonym, unlike Haskell's bare `type`), ` exposing (` (how both `module` headers and `import` statements restrict their export/import list in Elm), or a column-zero top-level type signature using a single ` : ` (e.g. `view : Model -> Html Msg`, distinct from Haskell's double-colon `::`) — markers not used by any sibling plugin; placed just after `nim` in `CORE_PLUGINS`, no ordering constraint against a specific sibling since it has no overlapping markers |
| Vue single-file component | `crates/plugins/vue` | #83 | Sniffs by a top-level `<template>` block paired with a top-level `<script>` block — neither tag alone is unique to Vue (native HTML5 also defines `<template>`, and `<script>` is ordinary HTML/XML), but this project's `html` plugin sniffs by document-structure tags (`<!doctype html`, `<html`, `<head>`, `<body>`, `<title>`) a Vue SFC does not carry, so the combination is not claimed by it first; placed just ahead of `html`/`xml` in `CORE_PLUGINS`, right after `svg` (no ordering dependency, kept adjacent as another markup-based document format). Extracts the file's top-level section-opening tags (`template`, `script setup`, `style scoped`, ...) in source order alongside the raw content, mirroring this project's other source-language plugins' own declaration-extraction pattern |
| Svelte component | `crates/plugins/svelte` | #84 | A Svelte component has no `<template>` wrapper the way a Vue SFC does (its markup lives at the top level), so this plugin instead sniffs Svelte's own template-language syntax: a `{#if `/`{#each `/`{#await ` block opener, a `{@html `/`{@debug ` tag, a `{:else`/`{:then`/`{:catch` continuation, a `<script context="module">` block, or a top-level `$:` reactive statement — markers not used by any sibling plugin. A real component's `<script>` block commonly also contains the JavaScript/TypeScript plugins' own bare `import `/`export ` markers, so this plugin is placed just ahead of `typescript` (and thus `javascript`) in `CORE_PLUGINS`, claiming genuine Svelte files by its own stronger markers first, mirroring the PHP plugin's own precedent for this kind of placement. Extracts the file's top-level section-opening tags (`script`, `script context="module"`, `style`, ...) in source order alongside the raw content, mirroring `vue`'s own extraction pattern |
| Ada source | `crates/plugins/ada` | #87 | Sniffs by `with Ada.` (a with-clause naming the Ada standard library, a vocabulary no other sniffed language shares), `end record;` (closing an Ada record type definition), a bare `begin` line opening a subprogram's statement part (distinct from Ruby's own bare `end` check, which never matches Ada's semicolon-terminated `end;`/`end NAME;`), or a `procedure`/`function`/`package`(` body`) construct header ending its line in a bare ` is` — markers not used by any sibling plugin; placed just after `fortran` in `CORE_PLUGINS` as another legacy procedural language, no ordering constraint against a specific sibling since it has no overlapping markers. The construct-header check doubles as declaration extraction, listing each top-level `procedure`/`function`/`package` name in source order alongside the raw content, mirroring this project's other source-language plugins' own declaration-extraction pattern |
| Crystal source | `crates/plugins/crystal` | #88 | Crystal's syntax otherwise closely resembles Ruby's (`def`/`end`/`class`), so this plugin instead sniffs Crystal-only vocabulary: a `crystal` shebang, the `getter`/`setter`/`property` built-in property macros (distinct from Ruby's `attr_accessor`/`attr_reader`/`attr_writer` and Objective-C's `@property`), a top-level `lib ` C-binding block, a leading `@[` annotation, or `uninitialized`/`pointerof(`. Deliberately does not sniff a bare `struct `/`macro `/`fun ` keyword, since the Rust/Julia, Nim, and Kotlin plugins respectively already claim those; placed just ahead of `ruby` in `CORE_PLUGINS` so it claims genuine Crystal files by its own stronger markers first |
| VB.NET source | `crates/plugins/vbnet` | #90 | Sniffs by VB.NET's own two-word block closers (`End Sub`, `End Function`, `End Module`, `End Class` — no sibling plugin's language closes a block this way), the `Imports System` module-level import (distinct from C#'s semicolon-terminated `using System;` and Java's lowercase `import java.`), the ` As New ` object-instantiation operator, or a bare `Dim ` variable declaration — markers not used by any sibling plugin; placed just after `csharp` in `CORE_PLUGINS` as another .NET-hosted language, no ordering constraint against a specific sibling since it has no overlapping markers. Extracts each top-level `Sub`/`Function` procedure name in source order alongside the raw content, mirroring this project's other source-language plugins' own declaration-extraction pattern |

## Rejected / infeasible

| Format | Issue | Reason |
| --- | --- | --- |
| `.dmg` (Apple UDIF disk image) | #70 | No usable sniff signature: its only fixed magic is a `koly` trailer at the *end* of the file (the body between header and trailer is an arbitrary wrapped filesystem), but `PluginCore::sniff` only ever sees a bounded prefix from the *start* of the file — no sniff-window size, however large, can see a trailer magic. Scoped out of the `disk-image` plugin (#70) which implements `.iso` only; revisit only if `PluginCore::sniff` ever gains a way to inspect a file's end |

## Maintaining this file

- **Built**: when a plugin's PR merges, add a row here with its format,
  crate path, and issue number, in the same PR or a prompt follow-up.
- **Rejected**: when a work order concludes a format can't reasonably
  become a plugin, add a row here with the format, its issue number, and
  a one-line reason, then close the issue without a PR. Do not reopen or
  retry a rejected format's work order unless explicitly asked to.
- Before proposing or starting any plugin work order, check both
  sections here, plus `gh issue list --label work-order --state all`
  for one already filed on the same format.
