/target

# Editor / OS
.vscode/
.idea/
*.swp
Thumbs.db
.DS_Store

# Local Model Context Protocol session state (cipher). Anchored to the
# repository root: unanchored, `data/` matches a directory of that name at
# any depth, and it silently swallowed `samples/purescript/src/Data/`,
# whose fixtures were then never committed at all.
/data/
